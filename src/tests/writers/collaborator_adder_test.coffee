describe "CollaboratorAdder", ->
  it "should add a new user", (done) ->
  it "should notify the existing users", (done) ->
  it "should email the existing users", (done) ->
  it "should notify the new user", (done) ->
  it "should email the new user", (done) ->
